{
  "version": 2,
  "buildCommand": "pnpm --filter @d-chemistry/web run build",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "outputDirectory": "apps/web/.next",
  "ignoreCommand": "bash -c 'if [ \"$VERCEL_ENV\" = \"production\" ]; then exit 1; else exit 0; fi'",
  "regions": ["bom1"]
}
